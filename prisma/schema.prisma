// Prisma schema for Collaboreum Platform
// Define data source and generator; models will be added in subsequent migrations.
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
